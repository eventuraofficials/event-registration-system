# =================================================================
# EVENT REGISTRATION SYSTEM - PRODUCTION ENVIRONMENT
# =================================================================
#
# DEPLOYMENT INSTRUCTIONS:
# 1. Copy this file to .env on your production server
# 2. Update all values with your production settings
# 3. NEVER commit .env file with real secrets to git
# =================================================================

# Server Configuration
PORT=10000
HOST=0.0.0.0
NODE_ENV=production

# Application URL - Your deployed domain
# Update this with your actual production URL (e.g., Render, Railway, etc.)
APP_URL=https://your-app-name.onrender.com

# Database Configuration (SQLite)
DB_PATH=./data/events.db

# JWT Secret - MUST BE CHANGED IN PRODUCTION
# Generate a new secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARACTERS_LONG

# Backup Configuration
AUTO_BACKUP=true
BACKUP_INTERVAL_HOURS=24

# CORS Configuration
# Set to your production domain
CORS_ORIGIN=https://your-app-name.onrender.com

# Rate Limiting (more restrictive in production)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (Optional - for sending QR codes)
EMAIL_ENABLED=false
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_FROM=Event Registration <noreply@yourdomain.com>

# Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# =================================================================
# DEPLOYMENT PLATFORMS - Uncomment the one you're using
# =================================================================

# RENDER.COM (Recommended - Free tier available)
# 1. Create new Web Service on render.com
# 2. Connect your GitHub repository
# 3. Build Command: npm install
# 4. Start Command: npm run production
# 5. Add Environment Variables in Render dashboard

# RAILWAY.APP (Alternative - Free trial)
# 1. Create new project on railway.app
# 2. Connect your GitHub repository
# 3. Railway will auto-detect Node.js
# 4. Add Environment Variables in Railway dashboard

# HEROKU (If you prefer)
# 1. Create Heroku app
# 2. Set buildpack: heroku/nodejs
# 3. Deploy: git push heroku main
# 4. Set env vars: heroku config:set KEY=value
